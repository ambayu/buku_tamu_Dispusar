# ==========================================
# COPY FILE INI MENJADI .env DI VPS
# ==========================================
# Command: cp .env.example .env
# Kemudian edit dengan credentials VPS Anda

# ==========================================
# ENVIRONMENT CONFIGURATION
# ==========================================
# Set to 'production' di VPS untuk keamanan maksimal
# Set to 'development' hanya untuk local development
APP_ENV=production

# Database Configuration - Buku Tamu
# PENTING: Gunakan 127.0.0.1 bukan localhost untuk VPS Linux
DB_HOST=127.0.0.1
DB_USER=your_database_user
DB_PASS=your_strong_password_here
DB_NAME=simc8935_bukutamu

# Database SKM
DB_HOST_SKM=127.0.0.1
DB_USER_SKM=your_database_user
DB_PASS_SKM=your_strong_password_here
DB_NAME_SKM=simc8935_skm

# Database Perpustakaan
DB_HOST_PERPUS=127.0.0.1
DB_USER_PERPUS=your_database_user
DB_PASS_PERPUS=your_strong_password_here
DB_NAME_PERPUS=simc8935_perpustakaan

# ==========================================
# SECURITY SETTINGS
# ==========================================
# Generate random string untuk production (minimal 32 karakter)
# Contoh: openssl rand -base64 32
CSRF_TOKEN_NAME=csrf_token
SESSION_LIFETIME=3600

# ==========================================
# CATATAN PENTING UNTUK PRODUCTION:
# ==========================================
# 1. Gunakan password database yang kuat (min 16 karakter, kombinasi huruf/angka/simbol)
# 2. Pastikan APP_ENV=production
# 3. Jangan pernah commit file .env ke Git
# 4. Set permission file .env: chmod 600 .env
# 5. Backup file .env di tempat aman

# Security Settings
SESSION_LIFETIME=3600
CSRF_TOKEN_NAME=csrf_token

# Environment (development/production)
APP_ENV=production
